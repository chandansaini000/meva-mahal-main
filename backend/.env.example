NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# The local instance shown in the project screenshot uses database `meva-mahal`.
# Replace YOUR_POSTGRES_PASSWORD; URL-encode special characters in it.
DATABASE_URL=postgresql://postgres:chandanji123@localhost:5432/meva-mahal
DATABASE_SSL=false

JWT_SECRET=change_this_to_a_long_random_string
SESSION_SECRET=change_this_too

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback
